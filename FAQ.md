# 常见问题 FAQ

## 🚀 部署相关

### Q: 支持哪些架构？
A: 支持 `linux/amd64`（X86）和 `linux/arm64`（ARM），Docker会自动选择对应架构的镜像。

### Q: 怎么修改端口？
A: 修改docker run命令中的端口映射，如 `-p 80:8080` 表示外部80端口映射到容器8080。

### Q: 容器无限重启怎么办？
A: 查看日志 `docker logs doro-panel`，常见原因：
- 数据目录权限问题：确保挂载的目录可写，或使用Docker卷 `doro_data`
- 端口被占用：修改端口映射
- 内存不足：增加服务器内存

### Q: 数据会丢失吗？
A: 数据存储在Docker卷 `doro_data` 中，只要不删除卷，重建容器数据不会丢失。

### Q: 怎么备份数据？
A: 
```bash
docker run --rm -v doro_data:/data -v $(pwd):/backup alpine tar czf /backup/doro-backup.tar.gz -C /data .
```

### Q: 怎么恢复数据？
A:
```bash
docker run --rm -v doro_data:/data -v $(pwd):/backup alpine tar xzf /backup/doro-backup.tar.gz -C /data
docker restart doro-panel
```

---

## 👤 账号相关

### Q: 默认账号密码是什么？
A: 默认账号：`admin` / `admin`，首次登录后请及时修改密码。

### Q: 忘记密码怎么办？
A: 
- 如果是admin账户：删除数据卷重新初始化（会丢失所有数据），或使用数据库工具直接修改密码
- 如果是普通用户：管理员可以在「系统设置」→「用户管理」中重置密码（仅admin账户可操作）

### Q: admin账户可以修改用户名吗？
A: 不可以，admin账户（user_id=1）不允许修改用户名，只能修改密码，这是为了安全考虑。

### Q: 怎么注册新用户？
A: 管理员在「系统设置」→「注册码」中生成注册码，新用户使用注册码注册。

### Q: 普通管理员和admin有什么区别？
A: 
- **普通管理员**：可以创建用户、删除用户、管理系统设置
- **admin超级管理员**：除了普通管理员的权限外，还可以修改其他用户密码、升级/降级管理员角色
- 只有admin账户（user_id=1）才有修改其他用户密码和角色的权限

---

## 🎨 主题与外观

### Q: 怎么切换主题？
A: 登录后，在「系统设置」→「主题」中切换内置主题，或上传主题插件。

### Q: 怎么上传主题插件？
A: 在「系统设置」→「插件」中上传主题插件包，支持应用、卸载、删除。

### Q: 更新后主题样式没变化？
A: 可能是浏览器缓存，按 `Ctrl+F5` 强制刷新，或在「系统设置」中点击「清除缓存」。

### Q: 立绘怎么开启？
A: 在「系统设置」→「立绘」中开启左右立绘，新安装默认关闭立绘。

### Q: 可以上传自己的立绘吗？
A: 可以，单用户最多上传30张立绘。

---

## 📊 功能相关

### Q: 股票行情不更新？
A: 检查容器网络是否能访问 `hq.sinajs.cn`，国内服务器一般正常。

### Q: 怎么添加上证指数？
A: 在股票管理中输入 `000001` 或 `sh000001`，系统会自动识别为上证指数。

### Q: 支持哪些股票代码？
A: 
- A股：`600519`（贵州茅台）、`000001`（上证指数）、`399006`（创业板指）
- 港股：`00700`（腾讯控股）、`HSI`（恒生指数）
- 常见指数代码会自动识别

### Q: 天气显示定位失败？
A: 可在天气设置中手动输入城市名称。

### Q: 网站favicon获取失败？
A: 自动切换为纯色文字图标，可自定义背景颜色。

### Q: 怎么给分类上锁？
A: 编辑分类时勾选「上锁」，解锁时需要输入密码，一次解锁所有上锁分类。

### Q: 隐藏的网站怎么显示？
A: 隐藏的网站仅登录后可见，带🔒标记，未登录时不显示。

---

## 💾 数据备份与恢复

### Q: 怎么备份网站配置？
A: 在「系统设置」→「数据备份」中导出CSV文件，包含分类、网址、名称、简介。

### Q: 备份包含哪些内容？
A: 仅包含除首页外的其他分类站点，不包含首页常用站点、用户设置、上传的素材等。

### Q: 怎么恢复网站配置？
A: 在「系统设置」→「数据备份」中导入CSV文件。

### Q: 恢复出厂设置会删除什么？
A: 
- 会删除：所有网站、分类、用户设置、股票、便签等
- 不会删除：用户上传的壁纸、表情包、立绘等素材
- 仅管理员可操作，需要输入 `RESET` 确认

### Q: 恢复出厂设置后怎么删除上传的素材？
A: 需要手动到服务器数据目录删除，Docker卷路径为 `/var/lib/docker/volumes/doro_data/_data/`

---

## 🔌 插件相关

### Q: 插件会被备份吗？
A: 不会，插件完全隔离，不参与数据备份和恢复出厂设置。

### Q: 每个用户的插件是独立的吗？
A: 是的，每个用户上传的插件独立，只能自己使用和管理。

### Q: 怎么删除自己上传的插件？
A: 在「系统设置」→「插件」中，自己上传的插件可以删除，系统内置插件不可删除。

---

## 🐛 其他问题

### Q: 怎么查看系统日志？
A: 管理员登录后，在「系统设置」→「日志」中查看，包含登录信息和操作记录。普通用户看不到日志。

### Q: 日志包含什么内容？
A: 包含用户登录信息（用户名、时间、IP）、操作记录（修改密码、切换角色等）、系统错误信息。

### Q: 日志会自动清理吗？
A: 是的，最多保留500行，自动截断旧日志。

### Q: 怎么提交bug反馈？
A: 在GitHub项目的Issues页面提交，附上截图、日志和复现步骤。

### Q: 有问题怎么联系作者？
A: 通过GitHub Issues提交，或在Docker Hub页面留言。
