# 更新日志

## v1.1 (2026-09-06)

### ✨ 新增功能

#### 👑 admin超级管理员权限管控
- **修改其他用户密码** - 仅admin账户（user_id=1）可重置其他用户密码，不需要旧密码，需要两次确认
- **切换用户角色** - 普通用户 ↔ 管理员，一键切换
- **三级权限体系** - 普通用户 / 普通管理员 / admin超级管理员
- **admin账户保护** - 不可删除、不可修改用户名、不可修改角色
- **权限隔离** - 其他管理员无权修改其他用户密码和角色

#### 🎯 默认启动模式设置
- 系统设置中可选择默认启动模式（普通模式 / 极简模式）
- **不同用户可设置不同的默认模式**，按用户隔离
- 打开网页时自动跳转到用户设置的模式
- 删除了"跟随模式"，只保留普通模式和极简模式

#### 🔗 左下角GitHub项目链接
- 版本号下面添加GitHub项目链接
- 使用GitHub官方Octocat猫图标
- 文字显示「GitHub · 项目地址」，点击新窗口打开

#### 👤 左下角显示当前登录账号
- 登录后显示当前登录用户名（不再显示"系统设置"）
- admin超级管理员：**金色**显示
- 普通管理员：**粉色**显示
- 普通用户：正常颜色显示
- 鼠标悬停显示提示：「当前账号：xxx（点击退出登录）」

#### 🔒 分类上锁功能
- 可给分类标签上锁，防止离开座位时别人偷看网站
- 解锁需要输入当前账号密码
- **输入一次密码即可解锁所有上锁的分类**
- 取消上锁也需要再输一遍密码

#### 📋 系统日志功能
- 记录用户登录信息和登录时间
- **只有管理员可以查看日志**，普通用户看不到
- 记录修改密码、切换角色等敏感操作

#### 📅 日历关闭按钮
- 日历弹窗右上角添加 ✕ 关闭按钮
- 点击即可关闭日历

#### 💾 用户数据存储限制
- **壁纸**：单用户最多 15 个
- **表情包**：单用户最多 20 个（默认 7 个不计入）
- **立绘**：单用户最多 30 个
- 用户上传的数据单独保存，**恢复出厂设置时不删除用户图片**
- 如需删除图片数据，需手动到服务器路径删除

#### 🔌 插件系统（预留）
- 插件功能与其他功能**完全隔离**
- 备份和恢复出厂设置**不影响插件**
- 每个用户的插件独立，可上传/删除自己的插件
- 支持主题插件和功能插件（预留样板，后期开发）

---

### 🔧 修复

#### 🚀 容器启动失败（严重）
- **修复gunicorn多worker竞态条件导致容器无限重启**
- users、categories、sites表的INSERT改成 `INSERT OR IGNORE`
- `db.init_db()` 加异常捕获
- 两个worker同时启动时不再因重复插入admin用户而崩溃

#### 📈 股票指数识别（严重）
- **修复输入 `000001` 被误识别为平安银行的问题**
- 新增常见指数代码映射表 `INDEX_CODES`
- 搜索和添加都优先匹配指数
- 支持的指数：上证指数(000001)、上证50(000016)、沪深300(000300)、中证500(000905)、科创50(000688)、深证成指(399001)、创业板指(399006)等
- 支持带前缀输入（如 `sh000001`）

#### 🎨 模式切换壁纸和模块加载（严重）
- **彻底修复默认极简模式下壁纸不显示的问题**
- **修复切换回普通模式时全部加载失败的问题**
- **修复切换回普通模式时天气和股票窗口加载失败的问题**
- 修复 `window.addEventListener('load', ...)` 在 `window.onload` 之后添加永远不会触发的问题
- 退出极简模式时彻底清除所有背景样式（`!important`）
- 退出极简模式时重新加载天气、股票、新闻、便签数据
- 退出极简模式时恢复立绘显示

#### 🖼️ 纯色图标显示
- 修复自定义颜色图标在选取窗口不显示的问题
- 修复内网模式下自定义颜色图标显示错误并添加失败的问题
- 修复极简模式搜索栏自定义颜色图标不显示的问题

#### 💾 备份导入导出
- 修复导出备份后再导入面板整体失效的问题
- 修复立绘、表情包等没有保存的问题
- **重构数据备份功能**：只备份网站和分类，方便重装服务器后快速导入网址
- 导入导出只看选项卡名称，匹配排除emoji图标
- 首页「常用站点」不参与备份，永远排除

#### 🏠 首页常用站点重构
- 首页不再是手动添加网址的页面
- **改成UI窗口选取其他分类下已有的图标**
- 保留内网和外网切换功能
- 首页删除图标**不影响**其他分类的原图标
- 极简模式搜索只匹配除首页之外其他分类的图标
- 16:10大UI，支持搜索，图标小一点方便选择
- 添加保存开关，避免误操作

#### 🎛️ 其他UI和功能修复
- 修复霓虹像素主题下系统面板位置错乱的问题
- 修复主题切换后部分颜色无法调节的问题
- 修复透明度进度条数值不实时更新的问题
- 修复其他两个主题下壁纸无法切换生效的问题
- 修复其他两个主题下极简模式壁纸无法切换的问题
- 修复密码输入框字体颜色问题（白色背景黑色字体）
- 修复添加网站后图标不立即显示的问题（需要刷新）
- 修复隐藏网站后无法添加到内网的问题
- 修复日历弹窗没有关闭按钮的问题

---

### 🎨 UI优化

- **侧边栏底部按钮布局优化**：用户按钮 + 设置按钮放在同一行，更紧凑好看
- **紧凑按钮样式**：减小内边距和字体，文字正常显示不换行
- **编辑模式按钮更直观**：从「✏️ 编辑」改成「✏️ 编辑布局」，加title提示
- **移除点击空白处关闭窗口**：避免输入网址时误触，只能点关闭按钮关闭
- **隐藏网站样式优化**：只保留🔒标记，移除灰度效果
- **左下角布局优化**：显示当前登录用户名 + GitHub项目链接
- **用户账户窗口**：点击用户按钮弹出账户信息，可退出登录

---

### 🔒 安全增强

- admin账户不可删除、不可修改用户名、不可修改角色
- 其他管理员无权修改其他用户密码和角色
- 所有敏感操作都有权限校验
- 用户数据按用户隔离存储
- 分类上锁功能，防止隐私泄露

---

### 📊 性能优化

- favicon服务器端缓存，减少重复请求
- 股票行情缓存，减少API调用
- 导入优化，避免超时
- 默认图标内置到镜像，首次安装即可显示

---

## v1.0 (2026-09-05)

### ✨ 主要功能
- **多用户系统** - 支持多用户数据隔离，管理员权限管控
- **三套内置主题** - 默认粉紫渐变、机甲科技、霓虹像素
- **立绘系统** - 左右独立立绘，支持轮换、大小位置调节，默认5张Dorothy立绘
- **表情包系统** - 默认7张表情包，用户可上传自定义
- **股票行情** - 实时A股/港股行情，支持上证指数等指数
- **实时天气** - 自动IP定位或手动设置城市
- **热点资讯** - 每日60秒新闻，自动刷新
- **分类上锁** - 支持分类加密，一次解锁所有上锁分类
- **隐藏网站** - 仅登录后可见，带🔒标记
- **首页常用站点重构** - 从其他分类选取网站引用，支持内网/外网切换
- **数据备份** - 站点导入导出（CSV格式）
- **恢复出厂设置** - 仅管理员可操作，不删除用户上传素材
- **系统日志** - 管理员可查看登录日志和操作日志
- **清除缓存功能** - 一键清除浏览器缓存
- **多架构支持** - linux/amd64 + linux/arm64

### 🔧 修复
- 修复gunicorn多worker竞态条件导致容器无限重启的问题
- 修复默认图标不显示的问题（.dockerignore排除了favicons目录）
- 修复极简模式搜索栏自定义颜色图标不显示的问题
- 修复进度条数值不实时更新的问题
- 修复主题切换后部分颜色无法调节的问题
- 修复霓虹像素主题下系统面板位置错乱的问题
- 修复备份导入后页面加载失败的问题
- 修复添加网站后图标不立即显示的问题
- 修复密码输入框字体颜色问题
- 修复其他多个UI和功能bug

### 📊 性能优化
- favicon服务器端缓存，减少重复请求
- 股票行情缓存，减少API调用
- 导入优化，避免超时

---

## 关于版本号

- **v1.0** - 第一个正式发布版本
- **v1.0.1** - 小bug修复
- **v1.1** - 新增重要功能
- **v2.0** - 大版本更新/架构重构

---

## 升级说明

### 从v1.0升级到v1.1

```bash
# 停止并删除旧容器
docker stop doro-panel
docker rm doro-panel

# 拉取最新镜像
docker pull wxp0577/doro-panel:latest

# 重新启动（数据不会丢失）
docker run -d \
  --name doro-panel \
  -p 8086:8080 \
  -v /home/docker/doro-panel:/app/data \
  -e TZ=Asia/Shanghai \
  --restart unless-stopped \
  wxp0577/doro-panel:latest
```

> ⚠️ 升级后建议按 `Ctrl+F5` 强制刷新浏览器，或在「系统设置」中点击「清除缓存」
